#Demo using h2o server
======

To start server `$ h2o -m daemon`. This would expect a configuration file at etc/h2o.php

To provide a custom config file at invokation issue a command like `$ h2o -m daemon -c /path/to/config`
